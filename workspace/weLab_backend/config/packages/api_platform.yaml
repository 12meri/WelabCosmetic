api_platform:
    title: Hello API Platform
    version: 1.0.0
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']

    # swagger: // https://api-platform.com/docs/core/jwt/#configuration
    #     api_keys:
    #         apiKey:
    #             name: Authorization
    #             type: header